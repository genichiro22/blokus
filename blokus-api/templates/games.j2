<!DOCTYPE html>
<head>
</head>
<body>
    <p>Current games</p>
    {% if user.id != -1 %}
    <div>
        <input type="submit" value="Create New Game">
    </div>
    {% endif %}
    {% for game in games %}
    <div>
        <a href="./{{ game.id }}">
            {{ game.id }}
        </a>
    </div>
    {% endfor %}
</body>
</html>